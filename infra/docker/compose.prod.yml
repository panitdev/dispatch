services:
  api:
    image: ${IMAGE_NAMESPACE:?IMAGE_NAMESPACE is required}/api:${IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-dispatch}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-dispatch}
      API_PORT: "8080"
      RUST_LOG: ${RUST_LOG:-info}
      FRONTEND_ORIGIN: ${FRONTEND_ORIGIN:?FRONTEND_ORIGIN is required}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-}
      KRATOS_PUBLIC_URL: ${KRATOS_PUBLIC_URL:?KRATOS_PUBLIC_URL is required}
      KRATOS_BROWSER_URL: ${KRATOS_BROWSER_URL:?KRATOS_BROWSER_URL is required}
      DISPATCH_PUBLIC_URL: ${DISPATCH_PUBLIC_URL:?DISPATCH_PUBLIC_URL is required}
      HYDRA_PUBLIC_URL: ${HYDRA_PUBLIC_URL:?HYDRA_PUBLIC_URL is required}
      HYDRA_ADMIN_URL: ${HYDRA_ADMIN_URL:?HYDRA_ADMIN_URL is required}
      SNOWFLAKE_MACHINE_ID: ${SNOWFLAKE_MACHINE_ID:-1}
      SNOWFLAKE_NODE_ID: ${SNOWFLAKE_NODE_ID:-1}
    env_file:
      - path: /opt/dispatch/env/api.env
        required: false
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik
      - traefik.http.routers.dispatch-api.rule=Host(`${API_HOST:?API_HOST is required}`)
      - traefik.http.routers.dispatch-api.entrypoints=websecure
      - traefik.http.routers.dispatch-api.tls=true
      - traefik.http.routers.dispatch-api.tls.certresolver=letsencrypt
      - traefik.http.routers.dispatch-api.service=dispatch-api
      - traefik.http.services.dispatch-api.loadbalancer.server.port=8080
    expose:
      - "8080"
    networks:
      - dispatch
      - traefik
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-dispatch}
      POSTGRES_USER: ${POSTGRES_USER:-dispatch}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_INITDB_ARGS: --encoding=UTF8 --locale=C
    volumes:
      - postgres-data:/var/lib/postgresql/data
    expose:
      - "5432"
    networks:
      - dispatch
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

networks:
  dispatch:
    name: dispatch
    driver: bridge
  traefik:
    name: traefik
    external: true

volumes:
  postgres-data:
